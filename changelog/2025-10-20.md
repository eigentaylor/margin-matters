## Laplace
- Set the default mode selector to "Relative delta mode" on page load.
- Added a maximum of 12 states shown in the tipping points table (instead of 10).
- Added a std dev column to the tipping points table.