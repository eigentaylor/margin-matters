## Sim2028

- Split the campaign map's polling tooltip so each candidate gets its own row instead of cramming the whole D/R/(third-party)/Undecided share breakdown and the derived margin onto one crowded "Poll" line. The derived margin now sits in its own row right below the 2020/2024 history rows, followed by a row per candidate (using their actual names when a pick is active) and an Undecided row.
