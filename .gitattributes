# Auto detect text files and perform LF normalization
* text=auto

# These are fully generated by build_site.py from changelog/*.md and other
# source files - never worth hand-merging conflicts. Take either side on
# merge, then rerun `python3 build_site.py` and commit the regenerated output.
docs/changelog.html merge=ours
docs/last-updated.json merge=ours
